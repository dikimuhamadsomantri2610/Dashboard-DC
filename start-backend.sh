#!/bin/bash

# Masuk ke direktori backend (sesuaikan path lengkapnya)
cd /home/dashboard-dc/Dashboard-DC/backend

# Opsional: Load NVM kalau Abang pakai NVM untuk manage versi Node
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Jalankan backend
# Pakai 'npm start' atau langsung 'node server.js'
npm start